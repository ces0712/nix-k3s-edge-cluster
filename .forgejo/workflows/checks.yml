name: checks

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  checks:
    runs-on: self-hosted
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Run repo checks
        shell: bash
        run: |
          just check
